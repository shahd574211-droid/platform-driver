
 #Database
DATABASE_URL="postgresql://username:password@localhost:5432/driver_recruitment?schema=public"

# JWT Secret (generate a secure random string)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# WhatsApp webhook: secret for bot (optional; or use Bearer token)
WEBHOOK_SECRET="your-webhook-secret-for-whatsapp-bot"

# Dev only (never used when NODE_ENV=production): skip login, use guest user
# DISABLE_AUTH=true
